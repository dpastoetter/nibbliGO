# Copy to hf.oauth.properties (or set in local.properties) for OAuth sign-in.
# Create an app at https://huggingface.co/settings/applications
# Redirect URI: nibbli://oauth/huggingface
clientId=YOUR_HF_OAUTH_CLIENT_ID
redirectUri=nibbli://oauth/huggingface
